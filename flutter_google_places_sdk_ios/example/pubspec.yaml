name: flutter_google_places_sdk_android_example
description: Example for use with the android package

publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_google_places_sdk_ios:
    path: ../

dev_dependencies:

flutter:
  uses-material-design: true
